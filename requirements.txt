# Python 3.11. Pinned to the versions the reported results were produced with.
# PyTorch is listed without its CUDA suffix; install the build matching your driver,
# e.g.  pip install torch==2.2.2 --index-url https://download.pytorch.org/whl/cu121

torch==2.2.2
torchvision==0.17.2
numpy==1.26.4
pandas==2.2.2
scikit-learn==1.4.2
scipy==1.13.0
matplotlib==3.8.4
tqdm==4.66.4

# reading the Kilosort 4 / TVSD recordings
nixio==1.5.3

# shared core + per-neuron readout building blocks
neuralpredictors==0.3.0
